Drop the two cut-outs here:

- ostrich-peek.png — the portrait, neck up, looking at you (used bottom-left on the home)
- ostrich-run.png  — the running ostrich (used above GET FOUND in the empty state)

Transparent PNG, ~1024x1536. The pages hide the images until the files exist.
